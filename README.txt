To run flask app: 
First, make sure flask, other packages are installed (type pip install flask mysql-connector-python into terminal)
In a terminal type “python3 -m flask --app retail_auto_parts_system run”
Local host address URL should be printed
Copy this into your browser and the program should run properly
